# Mnist-CNN-Project
#### git command to add all changed files
git add .

#### git command to comit my chnages
git commit -m "initial config done"

#### to push my code to main branch
git push origin main

#### to run bash script
bash ./init_script.sh  #. gives permission to run the script

#### Creating project structure
touch app.py
mkdir src
mkdir models
touch Developers.md
touch src/train_model.py
touch src/test_model.py